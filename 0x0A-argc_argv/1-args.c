#include <stdio.h>
#include <stdlib.h>

/**
 * main - check the code
 * @argc: number of parameters on command line
 * @argv: array of strings (command line parameters)
 *
 * Return: Always 0.
 */
int main(int argc, char __attribute__ ((unused)) *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
